import random
Choose_Name = ["James","John","Mark","Rick"]
for i in range(1,4):
    chosen = random.choice(Choose_Name)
    print(chosen)
    to_remove = input('remove from list?')
    if to_remove == "Y":
        Choose_Name.remove (chosen)
    print(Choose_Name)
print(Choose_Name)
